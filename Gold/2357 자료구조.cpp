#include <iostream>
#include <list>
using namespace std;

int arr[100001];

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	int N, M, a, b;
	cin >> N >> M;
	for (int i = 1; i <= N; i++) 
		cin >> arr[i];

	for (int i = 1; i <= M; i++) {
		cin >> a >> b;

	}


}